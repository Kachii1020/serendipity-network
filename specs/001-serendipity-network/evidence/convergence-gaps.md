# Final convergence and open gaps

**Date**: 2026-08-29  
**Status**: 87/91 required tasks complete; four real Site Tools gates remain,
with two human-research tasks optional

`spec.md`, `plan.md`, `data-model.md`, `contracts/webmcp-tools.md`,
`test-matrix.md`, and `tasks.md` were compared with the implemented routes,
schemas, migrations, tools, UI, deployment configuration, and executable test
results.

## Reconciled facts

- Historical Phase 0 selected Chrome `direct` composition with `json-string`
  execution encoding. The production judge surface is now exactly five
  top-level Hub Site Tools because ChatGPT does not discover iframe tools.
- Hub plus Kiln/Nori/Loop are four active fixed production origins with exact
  allowlists, no wildcard previews, and functions pinned to `hnd1`.
- The data model is implemented by five migrations and passes 73/73 local pgTAP
  assertions.
- Dedicated Tokyo-region Supabase project `gwluomrqzulnuhpdmucr` has all five
  migrations and the seed. Protected reset is repeatable, restores nine slots,
  and projects inventory onto the current Tokyo service date at capacity 20.
- Exactly five product Site Tools are registered at the top-level Hub. Provider
  frames retain `allow="tools"` only for the Chrome cross-origin diagnostic;
  neither the UI nor evidence claims ChatGPT executes iframe tools.
- Manual and Site Tool provenance are carried separately through the Hub and
  Provider presentation bridges. Local Phase 0/product tests pass 27/27, while
  the current production in-app browser exposes no Site Tools runtime.
- Root README, credential-free CI, hardened ignore rules, Devpost copy, a
  sub-three-minute video script, and a frozen first-user study protocol exist
  locally. A visible license, public Git repository, uploaded video, and human
  study results do not yet exist.
- Versioned area-pack validation is implemented. Shibuya remains the sole
  active launch network; a structurally valid non-Shibuya candidate stays dark
  until exact-origin supply and every promotion gate pass.
- Fixed production reliability passes 20/20 sequential reset/search/hold/confirm
  receipts, all three Provider `HELD → CONFIRMED` transitions per run, every p95
  bound, 201 unique correlations, mandatory final reset, and a 20/20 post-reset
  read-only baseline.
- T095 passes the exact nine-preset matrix, one-action/shared-Intent contract,
  accessibility/mobile/200%-zoom checks, stable detailed alternatives, early
  demo/no-payment disclosure, final production 20/20 recheck, and reset.
- T101 closes deterministic narrow/zoom focus, Release mutation locking and
  status recovery, the reload-persistent 90-second compensation guard, and
  internal Provider-proof overflow. Final production passes UI completeness
  8/8, Confirm 20/20, Release 20/20 with zero Confirm requests, final reset, and
  read-only 20/20.

## Open tasks — not marked complete

1. **T082**: execute AE-001–012 three times in a Site Tools-enabled Sol/Terra
   session. Three production reloads in the available client exposed neither
   `document.modelContext` nor any Site Tools API.
2. **T085**: record the canonical production discover → hold → confirm workflow
   and observer proof through real Site Tools. The manual production workflow
   passed in approximately 76 seconds and the proof disclosure in approximately
   0.8 seconds, but those are not agent-driven evidence.
3. **T089**: automated provenance, delegated Provider diagnostic discovery,
   fail-closed behavior, and local secret-surface checks pass. Real ChatGPT
   `Available`/`Recently used` inspection after a mutation workflow remains
   required for the complete SEC-013/STL-009 claim.
4. **T090**: run the real Sol/Terra tool-selection ladder 3/3. Deterministic
   Chrome coverage does not substitute for model choice and consent behavior.
   Optional supporting research, not required completion gates:

5. **T093**: complete five consumer problem interviews and two independent
   Provider interviews using the frozen protocol.
6. **T096**: run five fresh unaided post-change consumer sessions and measure the
   frozen completion, time, comprehension, and SEQ outcomes.

## External submission gaps

- An owner-selected open-source license and public Git repository are required.
- The prepared demo script still needs to be recorded and uploaded as a public
  video under three minutes.
- The current in-app browser is not an eligible Site Tools evidence surface;
  the five-tool Sol/Terra ladder must run in a client where the API is actually
  available.
- No participant outcome is claimed from the blank first-user study protocol.

The separate generic Supabase account project remains untouched. Production
uses only `serendipity-network-prod`. Hub `DEMO_MODE=true` exposes a
secret-protected operator reset; Provider demo cancellation remains disabled.
No production mutation was run during the original reset/read-only rollout
check. A later authorized 20-run mutation attempt stopped during bounded
first-iteration diagnostics and restored the baseline after every attempt. A
later freshly authorized execution passed the complete gate and is recorded in
`production-mutation-reliability.md`.
